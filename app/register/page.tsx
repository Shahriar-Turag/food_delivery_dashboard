import Register from '@/components/homePage/Register';
import HomePage from '@/components/homePage/Register';
import React from 'react';

type Props = {};

const page = (props: Props) => {
	return (
		<>
			<Register />
		</>
	);
};

export default page;
